

################# socket server ################################

Build node modules
	npm install

Generate DIST
	tsc -v

Turn the server on(use one of the following commands)
	nodemon dist/
	node dist/

